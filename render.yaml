services:
  - type: worker
    name: luna-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python girlfriend_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
